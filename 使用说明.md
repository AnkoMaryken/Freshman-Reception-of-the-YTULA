# 烟台大学文新学院 · 新生入学常见问题集 — 编辑使用说明

本网站是**文学与新闻传播学院新生入学常见问题集**，基于 VitePress 构建。你只需要**编写 Markdown 文章**，保存后刷新即可看到效果；构建发布也很简单。

---

## 目录结构

```
vitepress-docs/
├── package.json                # 项目依赖（一般不用动）
├── scripts/build.mjs           # 构建脚本（一般不用动）
├── .gitignore                  # Git 忽略规则
├── .github/workflows/deploy.yml # 自动部署配置
├── 使用说明.md                  # 本文件
└── docs/
    ├── index.md                # 首页（内容引导页）
    ├── public/                 # 静态资源（Logo、favicon 放这里）
    ├── .vitepress/
    │   ├── config.mjs          # 站点配置（导航栏、标题等）
    │   ├── sidebar.mjs         # ⭐ 侧边栏配置（新增文章要改这里）
    │   └── theme/custom.css    # 自定义样式（一般不用动）
    ├── guide/                  # 栏目一：入学指南
    │   ├── welcome.md          # 欢迎新同学
    │   └── before-arrival.md   # 入学前准备
    ├── register/               # 栏目二：报到注册
    │   ├── report.md           # 报到流程
    │   └── documents.md        # 证件与材料
    ├── life/                   # 栏目三：学习生活
    │   ├── study.md            # 学习指南
    │   ├── dorm.md             # 宿舍与生活
    │   └── canteen.md          # 食堂与美食
    └── faq/                    # 栏目四：常见问题
        ├── register.md         # 报到类问题
        ├── study.md            # 学习类问题
        ├── life.md             # 生活类问题
        └── other.md            # 其他问题
```

**核心概念**：`docs/` 下每个 `.md` 文件就是一个页面；目录就是栏目的「分组」，对应侧边栏里的分组。

---

## 一、如何新增一篇文章

只需要 **2 步**：

### 第 1 步：创建 Markdown 文件

在对应栏目目录下新建文件，例如给「常见问题」加一篇「缴费类问题」：

**文件路径**：`docs/faq/fee.md`

```markdown
---
title: 缴费类问题
outline: deep
---

# 缴费类问题

## 一、学费缴纳

### Q: 学费怎么交？

**A:** 按学校通知，通过官方渠道缴纳……

### Q: 可以缓缴吗？

**A:** 家庭经济困难的同学可申请绿色通道……
```

> **文件名规范**：使用英文小写、短横线连接（如 `fee.md`、`school-bus.md`），不要用中文文件名。

### 第 2 步：在侧边栏登记

打开 `docs/.vitepress/sidebar.mjs`，在对应栏目的 `items` 数组里加一行：

```js
'/faq/': {
  base: '/faq/',
  items: [
    {
      text: '常见问题',
      collapsed: false,
      items: [
        { text: '报到类问题', link: '/faq/register' },
        { text: '学习类问题', link: '/faq/study' },
        { text: '生活类问题', link: '/faq/life' },
        { text: '缴费类问题', link: '/faq/fee' },   // ← 新增这一行
        { text: '其他问题', link: '/faq/other' },
      ],
    },
  ],
},
```

**完成！** 保存后左侧边栏会自动出现新链接。

---

## 二、如何编辑已有文章

直接用任何文本编辑器（推荐 VS Code）打开 `docs/` 下的 `.md` 文件修改即可。

### Markdown 速查

```markdown
## 二级标题

### 三级标题

**加粗文字**

- 列表项一
- 列表项二

1. 第一步
2. 第二步

> 引用块

[链接文字](目标地址)

![图片说明](/图片文件名.png)   <!-- 图片放在 docs/public/ 下 -->
```

### 问答模板（FAQ 推荐写法）

```markdown
### Q: 问题标题？

**A:** 答案内容……
```

### 特殊提示框

```markdown
::: tip 提示
提示信息
:::

::: warning 注意
警告信息
:::

::: danger 重要
重要提醒
:::
```

---

## 三、如何在首页添加栏目入口

首页 `docs/index.md` 的「新生必读」和「快捷入口」是**手动维护**的。新增文章后，如果想在首页露脸：

1. 打开 `docs/index.md`
2. 在 `新生必读` 列表或 `.quick-links` 区域加一行链接：

```markdown
## 新生必读

- [缴费类问题](/faq/fee) ← 新增

<div class="quick-links">
  <a href="/faq/fee">缴费类问题</a>  ← 新增
</div>
```

---

## 四、如何启动本地预览

```bash
cd vitepress-docs
npm install      # 首次运行
npm run dev      # 启动开发服务器
```

浏览器打开终端显示的地址（默认 `http://localhost:5173`），**修改文章后页面自动刷新**，无需手动重启。

---

## 五、如何发布（构建）

### 本地构建

```bash
cd vitepress-docs
npm run build
```

构建产物在 `docs/.vitepress/dist/`，把整个目录上传到任意静态托管（GitHub Pages / 腾讯云 COS / 阿里云 OSS / Nginx 等）即可。

### 自动部署（GitHub Pages）

项目已内置 `.github/workflows/deploy.yml`。首次配置分两步：

**第 1 步：在 GitHub 创建远程仓库**

1. 打开 https://github.com/new
2. 仓库名随意（如 `xin-sheng-guide` 或 `yanda-wenxin-guide`），选 **Public**（免费部署需要）
3. **不要勾选** "Add a README"（避免冲突），点 Create repository

**第 2 步：把本地仓库推送上去**（在项目目录执行）

```bash
# 把下面的 <你的GitHub用户名> 和 <仓库名> 换成你自己的
git remote add origin https://github.com/<你的GitHub用户名>/<仓库名>.git
git push -u origin main
```

**第 3 步：开启 GitHub Pages**

1. 仓库页面 → **Settings** → **Pages**
2. Source 选择 **GitHub Actions**（不要选 Deploy from a branch）
3. 等 1-2 分钟，Actions 跑完后即可访问：`https://<你的GitHub用户名>.github.io/<仓库名>/`

::: warning ⚠️ 重要：`base` 路径配置
GitHub Pages 部署在**子路径**（`/<仓库名>/`）下时，必须修改 `docs/.vitepress/config.mjs` 中的 `base`：

```js
// 仓库名是 Freshman-Reception-of-the-YTULA 时：
base: '/Freshman-Reception-of-the-YTULA/'
```

**否则页面会乱码**（HTML 正常但 CSS/JS 全部 404，因为没有样式）。

- 部署在 `https://用户名.github.io/仓库名/` → `base: '/仓库名/'`
- 使用**自定义域名** 或 部署在根路径 → `base: '/'`
:::

**以后更新内容只需三步**：
```bash
git add .
git commit -m "更新常见问题"
git push
```

推送后自动构建部署，无需手动操作。

---

## 六、如何绑定自定义域名

1. 在 `docs/public/` 下创建 `CNAME` 文件，内容写你的域名：

```
docs.your-school.edu.cn
```

2. 在 DNS 服务商处添加解析记录：

| 类型 | 主机记录 | 记录值 |
|------|----------|--------|
| CNAME | 你的子域名 | `你的用户名.github.io` |

3. 域名生效后即可通过自定义域名访问。

---

## 七、常见维护问题

### Q: 改了文章但页面没变？

**A:** 检查开发服务器是否还在运行；侧边栏改动需要重新加载页面（一般自动生效）。如果一直不刷新，重启 `npm run dev`。

### Q: 新增了文件但侧边栏没有链接？

**A:** 忘了在 `sidebar.mjs` 登记，回到「新增文章」第 2 步。

### Q: 链接 404？

**A:** 检查 `sidebar.mjs` 里的 `link` 是否以 `/` 开头、是否正确对应文件路径（`/faq/fee` 对应 `docs/faq/fee.md`），不要带 `.md` 后缀。

### Q: 想改网站标题/颜色？

**A:** 标题在 `config.mjs` 的 `title` 字段；颜色在 `theme/custom.css` 顶部的 `--vp-c-brand-*` 变量。

### Q: 想加一个全新栏目？

**A:** 以「缴费」栏目为例：
1. 创建 `docs/fee/` 目录和文章；
2. 在 `sidebar.mjs` 添加 `/fee/` 的侧边栏配置；
3. 在 `config.mjs` 的 `nav` 里加导航项。

---

> **最终原则**：一切内容以学院官方通知为准，本站仅作整理汇总，请勿把非官方信息当作权威依据。
